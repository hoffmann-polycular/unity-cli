name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # Fail-fast guard: the git tag MUST agree with every place in the source
  # tree that hard-codes a version. Without this, a tag push with a missed
  # version bump would build five binaries stamped vX.Y.Z against a tree
  # whose connector still says X.Y.(Z-1), and every install would explode
  # at runtime with a connector-version-mismatch error.
  #
  # Sources checked against the tag with the leading 'v' stripped (X.Y.Z):
  #   - unity-connector/package.json           "version": "X.Y.Z"
  #   - unity-connector/Editor/Heartbeat.cs     CONNECTOR_VERSION = "X.Y.Z"
  #   - flake.nix                              version = "X.Y.Z"
  #
  # The flake's injected main.Version derives from `version` above as
  # `-X main.Version=v${version}`, so it tracks the checked attr automatically
  # and needs no separate check here.
  verify-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Verify tag matches versions in source
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          TAG_VERSION="${TAG#v}"
          echo "Tag:             ${TAG}"
          echo "Tag (stripped):  ${TAG_VERSION}"

          PKG_VERSION=$(jq -r .version unity-connector/package.json)
          HB_VERSION=$(grep -oP 'CONNECTOR_VERSION\s*=\s*"\K[^"]+' unity-connector/Editor/Heartbeat.cs)
          FLAKE_VERSION=$(grep -oP 'version\s*=\s*"\K[^"]+' flake.nix | head -1)

          fail=0
          check() {
            local label="$1" got="$2" want="$3"
            if [ "$got" = "$want" ]; then
              echo "  OK:   $label = $got"
            else
              echo "::error::$label = '$got' does not match expected '$want'"
              fail=1
            fi
          }
          check "unity-connector/package.json"            "$PKG_VERSION"   "$TAG_VERSION"
          check "unity-connector/Editor/Heartbeat.cs"     "$HB_VERSION"    "$TAG_VERSION"
          check "flake.nix version"                       "$FLAKE_VERSION" "$TAG_VERSION"

          if [ "$fail" -ne 0 ]; then
            echo
            echo "::error::Refusing to release: bump every source version to ${TAG_VERSION} before tagging."
            exit 1
          fi

  build:
    needs: verify-tag
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: "1.24"

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          EXT=""
          if [ "$GOOS" = "windows" ]; then EXT=".exe"; fi
          VERSION="${GITHUB_REF_NAME}"
          go build -ldflags="-s -w -X main.Version=${VERSION}" -o "unity-cli-${GOOS}-${GOARCH}${EXT}" .

      - uses: actions/upload-artifact@v4
        with:
          name: unity-cli-${{ matrix.goos }}-${{ matrix.goarch }}
          path: unity-cli-*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: unity-cli-*
